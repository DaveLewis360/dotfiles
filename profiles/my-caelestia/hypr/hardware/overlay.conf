# Ezt a fájlt a dotswitch overlay kezeli — NE szerkeszd kézzel.
#
# A ~/dotfiles/shared/common.json "input" blokkja kerül ide profilváltáskor,
# egy "dotswitch managed" blokkban. Ott szerkeszd, ne itt.
#
# A hyprland.conf UTOLSÓKÉNT source-olja, hogy a közös beállítások
# felülírják a hardware/input.conf-ban lévő profil-specifikus értékeket.
#
# Megjegyzés: a Hyprland a generált .lua változatot tölti be ebben a
# profilban (hardware/overlay.lua), amit közvetlenül az overlay ír.
